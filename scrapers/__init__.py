"""DataWiz collection engine.

A single shared collector plus per-outlet configuration (config.py). Adding an
outlet is a config entry, not a new module — this guarantees uniform output
across owners. See docs/SCRAPER_INTEGRATION_GUIDE.md.
"""
